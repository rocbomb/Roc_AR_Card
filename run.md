# AR动漫卡片召唤器 - 运行指南

## 快速启动

双击 `run.bat` 即可启动 HTTPS 服务器。

## 访问方式

### 电脑浏览器
```
https://127.0.0.1:3000
```

### 手机浏览器
1. 确保手机和电脑在 **同一 WiFi** 下
2. 访问：`https://电脑IP:3000`（IP 会在启动时显示）
3. 浏览器提示"不安全" → 点击 **高级** → **继续访问**
4. 允许摄像头权限

## 手动启动命令

```bash
# 进入项目目录
cd e:\SelfWork\ARCard

# 启动 HTTPS 服务器
npx http-server -S -C cert.crt -K cert.key -p 3000
```

## 首次运行

如果没有证书文件（cert.crt、cert.key），需要先生成：

```bash
npx mkcert create-ca
npx mkcert create-cert
```

## 常见问题

### 手机无法访问？
- 检查是否在同一局域网
- 关闭电脑防火墙或允许 3000 端口

### 摄像头不工作？
- 必须使用 HTTPS（http 无法调用摄像头）
- 检查浏览器是否已授权摄像头权限

### 模型不显示？
- 确保卡片图片清晰、光线充足
- 按 F12 查看控制台错误信息
